# Downloader
This repo contains both the downloader library and [cli app](cmd) for 
downloading multiple files at the same time

# CLI App
See the [CLI readme](cmd) for more information on how to install and use it.
